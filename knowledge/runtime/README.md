# 知识运行记录

本目录保存旧 Runner 生成的治理状态，包括反馈账本、追加式活动日志、live-mode cursor 和可选 jury 结果。这些文件是运行证据，不是知识卡片。

Agent 不得通过直接编辑这里的文件绕过摄取流程或 Gate。当前 TypeScript 运行时的业务事实保存在 `.workpanel/` 下的 SQLite Registry 与 CAS；本目录仅作历史迁移和审计参考。

<details lang="en">
<summary>English summary</summary>

This directory contains legacy operational evidence, not knowledge cards. Agents must not edit these files to bypass ingestion or publication gates. The current runtime stores authoritative business facts in the SQLite Registry and CAS under `.workpanel/`.

</details>
