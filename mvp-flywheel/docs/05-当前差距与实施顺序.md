# 当前差距与实施顺序

> 状态：Living gap analysis  
> 基线：2026-08-26，已包含远端提交 `8cfe6d2` 的新门禁实现，及后续 `778c633`/`26ff1d8`（SDD 文档 + 规格级测试）与错误码/验收补充。

## 1. 当前结论

当前代码已具备最小流水线、C/C++ 编译测试、基础修订、最小权限 sandbox、JSON holdout、至少 5 次重复评测的均值/方差/最差值、UNSTABLE/stopped 状态、完整 SHA-256 写保护、独立归因/修订落盘、统一错误码（契约 §6）与探针证据记录。DeepSeek demo 与桩测试仅作管道自检，不得作为生产合规证明（MOD-002/ROLE-006）。

## 2. 合规矩阵

| 规格 | 状态 | 当前证据 | 差距 |
|---|---|---|---|
| SYS-001/002 | CURRENT | `fw/runner.py` 闭环 + 状态机；`test_acceptance_extra.py::test_run_*` | 无 |
| SYS-003 | CURRENT | `run(initial_doc=...)` 支持既有知识启动，不调用知识生成 | 无（测试已覆盖） |
| MOD-001/002 | PARTIAL | `roles/providers.py` CodeAgentProvider/DeepSeekProvider；`build_llm_roles` 按配置构建；production fail-fast | codeagent CLI 实机未验证（无公司内网/凭据） |
| ROLE-001~004 | CURRENT | 三角色接口 + 确定性 `decide()`；Review 只读脱敏报告 | 无 |
| ROLE-005 | PARTIAL | 文件产物交接（knowledge/code/eval/attribution/corrections/protection）| LLM 调用输入清单审计未单独落盘 |
| KNOW-001 | CURRENT | `KnowledgeDoc.render()` 输出 OKF frontmatter（version/status/source_commit/sources） | 无 |
| KNOW-003 | CURRENT | `assert_no_source_leak` 确定性拦截；桩实现仅测试用 | 无 |
| KNOW-004/005 | CURRENT | `apply_revision` 不可变 + 版本+1；候选区写入 run artifacts，仅 publish 写公共 knowledge_dir | 无 |
| EVAL-001/002/005 | CURRENT | `eval/` 编译、测试主判、相似度诊断；`decide` 永不读 similarity | 无 |
| EVAL-003 | CURRENT | `ZERO_CASES` → stopped + INVALID_EVALUATION | 无 |
| EVAL-004 | CURRENT | `Config.pass_threshold` 可配置；`EvalReport.passes(threshold)` 显式传阈值 | 无 |
| EVAL-006/007 | CURRENT | `repeat_eval=5`；`_summarize_reps` mean/variance/min；`unstable` 判定；`test_error_codes.py` 覆盖 | 无 |
| EVAL-008 | CURRENT | 报告含 `probe_evidence`（provenance/source_commit/evalset_version）；`test_acceptance_extra.py::test_report_contains_probe_evidence` | manifest golden_note 校验未自动化 |
| EVAL-009/010 | CURRENT | holdout 只聚合脱敏；`verified` 在 holdout 通过后设置 | 无 |
| EVAL-011 | CURRENT | native 无 holdout + require_holdout → stopped；`test_sdd_contracts.py::test_native_without_holdout_cannot_publish_when_required` | 无 |
| EVAL-012 | CURRENT | Review 无法修改 `compile_ok/confidence/decision`（只读报告副本） | 无 |
| SEC-001/005 | CURRENT | `fw/sandbox.py` resolve 后校验 + denied 集；错误码 SOURCE_ACCESS_VIOLATION | 符号链接专项测试待补 |
| SEC-002 | CURRENT | production 强制 `interfaces_dir`；`validate()` fail-fast | 无 |
| SEC-003/004 | CURRENT | 最小只读白名单（knowledge/interfaces）+ 独立写目录；public/private evalset 拆分 | 无 |
| SEC-006 | CURRENT | `fw/protection.py` 完整 64 位 SHA-256 前后校验；ProtectionMismatch | 无 |
| FLOW-001/003 | CURRENT | `decide` 含 pass/iterate/rollback/unstable/stopped + MAX_BUDGET/INCOMPLETE_FEEDBACK | 无 |
| FLOW-002 | CURRENT | rollback 恢复 `doc_history[-2]` 真实内容（`test_acceptance_extra.py::test_rollback_restores_persisted_content`） | 无 |
| FLOW-004/005 | CURRENT | Correction readlist 三字段；ledger append-only（history 保留，clear 不删审计） | 无 |
| OBS-001~004 | CURRENT | run ID 关联全部产物；报告含 schema_version/environment/repetitions/reason_codes | 无 |

## 3. 实施顺序

### P0-A：先修正裁判 ✅ 已闭合
- 评测统计模型（mean/variance/min + repetitions 明细 + repeat_generation 与 repeat_eval 分离）
- 统一门禁函数（阈值只来自 Config，删除固定 0.8）
- 状态机完备（unstable/stopped、零用例、预算耗尽、reason codes）
- holdout 发布规则（verified 在 holdout 后；native fail closed）

### P0-B：收紧信息与写保护 ✅ 已闭合
- 生产路径校验（production 强制 interfaces_dir）
- 最小权限沙箱（读写分离 + denied 集 + 错误码）
- 评测集分区（public/private_evalset_dir）
- 写保护（执行前后完整 SHA-256）

### P0-C：修正知识版本与审计 ✅ 已闭合
- 候选区（draft 写入 run artifacts）
- 真回滚（按 parent 恢复持久化内容）
- append-only ledger（clear 保留 history）
- 报告 v1（run ID、配置快照、环境、统计、reason codes、probe_evidence）

### P0-D：接入生产模型 ⏳ 部分
- ✅ 抽象 provider（providers.py）；DeepSeek 仅 experimental
- ⏳ codeagent CLI 实机适配验证（需要公司内网/凭据/GLM 5.1）
- ⏳ 角色输入清单审计（模型/prompt/输入文件清单/输出哈希）

## 4. 暂不进入 P0

- AST/数据流相似度；
- 多候选 pass@k；
- 自动覆盖率与 mutation score；
- 语义保持变换和评测集轮换；
- 性能/内存门禁；
- 上亿行代码的全库切分与调度。

## 5. 当前验证状态

- `python3 -m pytest tests/` → **87/87 通过**（含 tests/spec/ 错误码与验收场景）
- `python3 verify_sandbox.py` → PASS
- `python3 verify_isolation_layout.py` → PASS
- 真实模型端到端需公司 GLM 5.1 + codeagent CLI；当前环境仅 DeepSeek（experimental）与桩测试可跑。

